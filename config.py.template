# NewsSpider 配置文件模板
# 请将此文件复制为 config.py 并填写相应的配置信息

import os


# =================================================
# 配置网络代理，请填写自己的端口 
# =================================================
os.environ["http_proxy"] = "http://127.0.0.1:xxxx"
os.environ["https_proxy"] = "http://127.0.0.1:xxxx"
os.environ["all_proxy"] = "socks5://127.0.0.1:xxxx" 
# =================================================

class Config:
    """NewsSpider 配置类"""
    
    # GOOGLE API 配置
    GOOGLE_API_KEY = os.getenv("GOOGLE_API_KEY", "your_deepseek_api_key_here")
    
    # 文件路径配置
    PDF_FILE_PATH = "./News.pdf"
    
    # 目标网站配置
    TARGET_BASE_URL = "https://nbw.sztu.edu.cn/"
    TARGET_PAGE_URL = "https://nbw.sztu.edu.cn/list.jsp?urltype=tree.TreeTempUrl&wbtreeid=1029"
    
    # 请求配置
    USER_AGENT = "Mozilla/5.0"
    REQUEST_DELAY = 2  # 请求间隔时间（秒）
    
    # PDF 配置
    PDF_FONT_PATH = "/System/Library/Fonts/Supplemental/Songti.ttc"  # macOS 中文字体路径
    
    # AI 模型配置
    GEMINI_MODEL="gemini-2.5-flash"
    EMBEDDING_MODEL = "models/text-embedding-004"
    
    # 文本处理配置
    CHUNK_SIZE = 1000
    CHUNK_OVERLAP = 50
